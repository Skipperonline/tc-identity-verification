async function verifyWithMernis({ tckn, firstName, lastName, birthYear }) {
  // Eğer ki yetkili bir kurumsanız bu entegrasyonu kendi api bilgileriniz ile doldurmalısınız.
  return false;
}

module.exports = { verifyWithMernis };
